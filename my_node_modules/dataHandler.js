const fs = require('fs');

// utility fnction: reading json and returning object corresponding to it
function readData() {
    const jsonData = fs.readFileSync('serverdb.json', 'utf-8');
    return JSON.parse(jsonData);
}



const getAllFoods = () => readData();



function getFoodNames() {
    const data = readData();
    return data.map(item => item['Food (100g)']);
}


function test() {
    console.log('hello,test')
}


module.exports = { getAllFoods, getFoodNames, test };